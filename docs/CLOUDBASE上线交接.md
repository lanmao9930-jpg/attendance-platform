# CloudBase 上线交接

## 当前正式环境

- 腾讯云环境：`attendance-platform-d1b99a89a6e3`
- 地域：上海 `ap-shanghai`
- 套餐：体验版，到期时间 `2027-01-19 23:59:59`
- HTTP 云函数：`attendance-platform`
- 正式域名：`https://attendance-platform-d1b99a89a6e3.service.tcloudbase.com`
- 管理端：`https://attendance-platform-d1b99a89a6e3.service.tcloudbase.com/admin`

现场屏没有公开固定地址。管理员登录后点击“打开现场屏”或“复制现场屏地址”，得到带现场密钥的 `/display?key=...` 地址；这个地址只放在现场设备，不发给学生。学生只扫描动态二维码进入 `/student?token=...`。

## 云端资源

平台使用一个 HTTP 云函数承载网页和 API，避免跨域与多项目配置。数据分开保存：

| 资源 | 用途 |
| --- | --- |
| `attendance_schedules` | 当前启用的固定排班 |
| `attendance_schedule_history` | 旧排班版本的只读元数据与压缩分块 |
| `attendance_reviews` | 管理员考勤状态与备注 |
| `attendance_checkins` | 原始签到签退记录 |
| 云存储 `photos/` | 值班照片 |

云存储权限已核实为“仅创建者及管理员可读写”。学生端不会获得照片文件 ID，查看照片必须经过已登录的 `/api/photo` 管理员接口。

## 部署配置

`cloudbaserc.json` 固定环境、函数类型、运行时和忽略文件。真实管理员口令与二维码签名密钥保存在被 Git 和 CloudBase 部署包同时忽略的 `.env.local`，不会提交到仓库或上传到函数代码包。

正式口令已在上线验收后随机化，测试口令 `12345678` 已失效。需要再次轮换时运行 `node scripts\rotate-cloudbase-secrets.js` 后重新部署云函数；脚本只显示新管理员口令，不显示二维码签名密钥。

首次或重新部署：

```powershell
npx --yes -p @cloudbase/cli tcb fn deploy attendance-platform --httpFn --force --json
node scripts\cloudbase-setup.js route
```

只更新已有函数代码时：

```powershell
npx --yes -p @cloudbase/cli tcb fn code update attendance-platform --dir . --deployMode cos --json
```

发布完成后必须等待 `tcb fn list --json` 显示“部署完成”，并确认 `/api/health` 的版本号已经变化，不能只依据 CLI 的“上传成功”提示判断上线完成。

仅首次创建数据集合：

```powershell
node scripts\cloudbase-setup.js collections
```

部署后只读验证：

```powershell
$env:CLOUDBASE_ADMIN_PASSWORD="管理员口令"
node scripts\cloudbase-smoke-test.js
```

不要重复运行带 `CLOUDBASE_WRITE_TEST=1` 的照片写入测试，除非同时按测试输出清理对应记录和照片。

## 已完成验收

2026-07-24 已发布 `0.7.1` 并完成以下线上只读检查：

1. `/api/health` 返回 `200`、版本 `0.7.1`、存储模式 `cloudbase`，云函数状态为“部署完成”。
2. 学生表单职位选项为学生助理、副主管、主管、团长、实习生；值班类型为正常、调班，调班说明由前后端共同校验为必填。
3. 管理端人工复核已加入“请假”；排班导入区同时提供“更新当前固定排班”和“导入新的固定排班”，排班版本列表可查看只读历史。
4. 新排班启用后，上一版排班按每 200 条一个文档压缩保存在 `attendance_schedule_history`，管理端日常刷新只读取当前排班，避免历史数据持续消耗免费读额度。
5. 线上当前固定排班为 8533 条唯一的逐周排班记录、256 人、11 个中心，排班版本 1 个；原始签到签退为 5 条。这些数据均为发布前已有数据，本次验收没有新增、替换或删除排班、签到、审核或照片。
6. 线上只读预览 `2026年上半年重师就业服务团值班表.xlsx` 仍识别为 494 条周期排班、256 人、12 张工作表、0 条结构问题。
7. 隔离环境完整测试通过：学生申报跨时段调班自动归档为调班、管理端可复核为请假、新旧排班形成两个版本、旧签到标记为历史记录、受保护照片仍可读取。

2026-07-22 已完成以下线上检查：

1. `/api/health` 返回 `200`、版本 `0.6.0`、存储模式 `cloudbase`。
2. 未授权读取动态二维码返回 `401`。
3. 管理员口令登录成功，能读取排班与原始记录并生成受保护的现场屏地址。
4. 动态二维码能换取学生表单会话。
5. 学生首页、现场屏和学生表单均不包含管理端入口或考勤看板内容。
6. 生产环境只读预览真实文件 `2026年上半年重师就业服务团值班表.xlsx`，识别为 494 条循环排班、256 人、12 张工作表、0 条结构问题。
7. 单个中心文件允许导入；确认时只更新文件中出现的中心，并保留其他中心已有排班。
8. 横向课表可识别星期、节次、单周/双周、起止周、指定周前后和每月第几周等规则。
9. 管理端考勤审核支持按中心和周次筛选；签到按 08:30、10:30、14:30、16:30 四个开始时间自动判正常或迟到，提前签退、过时未签退和排班不匹配会标红。
10. 正式库当前为 494 条固定排班、3 条扫码记录。抽查第8周大数据中心得到40条排班考勤，其中1条扫码记录未匹配固定排班，已由系统自动标为异常。
11. 本次 `0.6.0` 上线验收仅执行读取和导入预览，没有新增、覆盖或删除线上排班、签到、审核或照片数据。

2026-07-20 首次上线时已另行完成固定排班、签到、照片和“调班”审核的完整写入闭环，测试数据与照片随后已清理。

真实手机仍应在正式使用前分别用校园 Wi-Fi 和手机流量扫码一次，这是对学校现场网络的最终验收，不涉及代码配置。

## 续期提醒

体验版不会自动续费。请在 `2026-12-19` 以后、`2027-01-19` 到期前进入 CloudBase 环境管理检查免费续期入口。不要主动升级付费套餐或开启按量付费；如免费政策发生变化，应先由学校决定是否承担费用。
